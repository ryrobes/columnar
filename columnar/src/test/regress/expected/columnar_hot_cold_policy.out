CREATE SCHEMA hybrid_policy;
SET search_path TO hybrid_policy, public;
CREATE TABLE run_state_hot (
  run_id INT NOT NULL,
  step_no INT NOT NULL,
  status TEXT NOT NULL,
  finished_at TIMESTAMPTZ
) USING heap;
CREATE TABLE run_state_cold
  (LIKE run_state_hot INCLUDING DEFAULTS)
  USING columnar;
CREATE OR REPLACE FUNCTION hybrid_policy.finished_run_selector(
  policy_name TEXT,
  hot_table REGCLASS,
  cold_table REGCLASS)
RETURNS TEXT
LANGUAGE plpgsql
SET search_path = pg_catalog
AS $$
DECLARE
  ready_run_ids TEXT;
BEGIN
  EXECUTE format(
    'SELECT string_agg(run_id::text, '','' ORDER BY run_id) ' ||
    'FROM (SELECT DISTINCT run_id FROM %s WHERE finished_at IS NOT NULL) ready',
    hot_table)
    INTO ready_run_ids;

  IF ready_run_ids IS NULL THEN
    RETURN NULL;
  END IF;

  RETURN format('run_id IN (%s)', ready_run_ids);
END;
$$;
CREATE OR REPLACE FUNCTION hybrid_policy.bad_selector()
RETURNS TEXT
LANGUAGE sql
AS $$ SELECT 'run_id = 1' $$;
INSERT INTO run_state_hot VALUES
  (1, 1, 'done', '2026-04-10 00:00:00+00'),
  (1, 2, 'done', '2026-04-10 00:00:00+00'),
  (2, 1, 'running', NULL),
  (3, 1, 'done', '2026-04-10 00:00:00+00');
SELECT columnar.create_archive_policy(
  'run_state_policy',
  'run_state_hot',
  'run_state_cold',
  'hybrid_policy.finished_run_selector(text,regclass,regclass)',
  'hybrid_policy.run_state_read');
 create_archive_policy 
-----------------------
 t
(1 row)

SELECT policy_name, read_view, delete_from_hot, enabled
FROM columnar.archive_policy
ORDER BY policy_name;
  policy_name    |         read_view         | delete_from_hot | enabled 
-----------------+---------------------------+-----------------+---------
 run_state_policy | hybrid_policy.run_state_read | t               | t
(1 row)

SELECT COUNT(*) FROM run_state_read;
 count 
-------
     4
(1 row)

SELECT columnar.run_archive_policy('run_state_policy');
 run_archive_policy 
--------------------
                  3
(1 row)

SELECT COUNT(*) FROM run_state_hot;
 count 
-------
     1
(1 row)

SELECT COUNT(*) FROM run_state_cold;
 count 
-------
     3
(1 row)

SELECT COUNT(*) FROM run_state_read;
 count 
-------
     4
(1 row)

SELECT policy_name, rows_moved, where_clause
FROM columnar.archive_run_log
ORDER BY id;
  policy_name    | rows_moved | where_clause  
-----------------+------------+---------------
 run_state_policy |          3 | run_id IN (1,3)
(1 row)

SELECT columnar.create_archive_policy(
  'disabled_policy',
  'run_state_hot',
  'run_state_cold',
  'hybrid_policy.finished_run_selector(text,regclass,regclass)',
  NULL,
  true,
  false);
 create_archive_policy 
-----------------------
 t
(1 row)

SELECT * FROM columnar.run_archive_policies();
  policy_name    | rows_moved 
-----------------+------------
 run_state_policy |          0
(1 row)

SELECT policy_name, rows_moved, where_clause IS NULL AS empty_where_clause
FROM columnar.archive_run_log
ORDER BY id;
  policy_name    | rows_moved | empty_where_clause 
-----------------+------------+--------------------
 run_state_policy |          3 | f
 run_state_policy |          0 | t
(2 rows)

SELECT columnar.create_archive_policy(
  'bad_policy',
  'run_state_hot',
  'run_state_cold',
  'hybrid_policy.bad_selector()');
ERROR:  selector function hybrid_policy.bad_selector must have signature (text, regclass, regclass) RETURNS text
CONTEXT:  PL/pgSQL function columnar.create_archive_policy(text,regclass,regclass,regprocedure,text,boolean,boolean) line 56 at RAISE
SELECT columnar.drop_archive_policy('disabled_policy');
 drop_archive_policy 
---------------------
 t
(1 row)

SELECT columnar.drop_archive_policy('run_state_policy', true);
 drop_archive_policy 
---------------------
 t
(1 row)

DROP SCHEMA hybrid_policy CASCADE;
NOTICE:  drop cascades to 4 other objects
DETAIL:  drop cascades to table run_state_hot
drop cascades to table run_state_cold
drop cascades to function finished_run_selector(text,regclass,regclass)
drop cascades to function bad_selector()
